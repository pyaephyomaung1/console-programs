package main

import "fmt"

func main() {
	// student()
	// rang()
	// calculate()
	// fibonacci()
	// sum := add(4, 2)
	// fmt.Println(sum)
	// greetUser("John Doe")
	// sum, err := ParseAndAdd("30", "40")
	// if err != nil {
	// 	fmt.Errorf("Error", err)
	// } else {
	// 	fmt.Println(sum)
	// }
	// sum, err := ParseAndAdd("30", "abc")
	// if err != nil {
	// 	fmt.Errorf("Error", err)
	// } else {
	// 	fmt.Println(sum)
	// }
	// quotient, remainder := divide(10, 2)
	// fmt.Print(quotient, remainder)
	fmt.Print(toUpperCase("Hello World"))
}
